# Thin client config. Routes, port (8080) and the rest are fetched from config-server
# (see configs/gateway-server/ in the Git repo).
spring:
  application:
    name: gateway-server
  profiles:
    active: local
  config:
    import: configserver:${CONFIG_SERVER_URI:http://localhost:8888}
